{
  "name": "atria-earning-system",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute atria-earning-db --remote --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
